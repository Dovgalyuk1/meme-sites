Сюда кладутся сохранённые страницы-референсы (Cmd+S → «только HTML»).
`node tools/refs.js` разберёт всё, что здесь лежит, и сложит результат
в `claude/refs/<имя>/`. Сами .html в гит не идут — в репозитории остаётся
разбор, а не чужой сайт.
